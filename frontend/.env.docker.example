# =============================================================================
# KISAN SAATHI - FRONTEND ENVIRONMENT CONFIGURATION (DOCKER)
# =============================================================================
# This is the Docker-specific environment configuration for the frontend
# Copy this file to frontend/.env and fill in your actual values
# =============================================================================

# Backend API URL
# For Docker: Use the host machine's localhost (accessible from browser)
VITE_SERVER_URL=http://localhost:5001

# For production deployment, replace with your actual backend domain:
# VITE_SERVER_URL=https://api.yourdomain.com
