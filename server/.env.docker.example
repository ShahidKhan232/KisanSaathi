# =============================================================================
# KISAN SAATHI - BACKEND ENVIRONMENT CONFIGURATION (DOCKER)
# =============================================================================
# This is the Docker-specific environment configuration for the backend
# Copy this file to server/.env and fill in your actual values
# =============================================================================

# -----------------------------------------------------------------------------
# SERVER CONFIGURATION
# -----------------------------------------------------------------------------
PORT=5001
NODE_ENV=production

# -----------------------------------------------------------------------------
# DATABASE CONFIGURATION (Docker)
# -----------------------------------------------------------------------------
# Use the Docker service name 'mongodb' instead of 'localhost'
MONGO_URI=mongodb://mongodb:27017/kisan_saathi

# -----------------------------------------------------------------------------
# AUTHENTICATION & SECURITY
# -----------------------------------------------------------------------------
# IMPORTANT: Generate a secure JWT secret for production!
# Run: openssl rand -base64 32
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production

# -----------------------------------------------------------------------------
# CORS CONFIGURATION (Docker)
# -----------------------------------------------------------------------------
# Allow requests from the frontend container
# For production, replace with your actual domain
CLIENT_ORIGIN=http://localhost:80,http://localhost:5173
CLIENT_URL=http://localhost:80

# -----------------------------------------------------------------------------
# AI SERVICE CONFIGURATION
# -----------------------------------------------------------------------------
# AI Provider: 'openai' or 'gemini'
AI_PROVIDER=gemini

# OpenAI API Key (required if AI_PROVIDER=openai)
OPENAI_API_KEY=sk-your-openai-api-key-here

# Google Gemini API Key (required if AI_PROVIDER=gemini)
# Get from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key-here

# Gemini Model to use
GEMINI_MODEL=gemini-1.5-flash

# -----------------------------------------------------------------------------
# EXTERNAL API CONFIGURATION
# -----------------------------------------------------------------------------
# Market Data API
MARKET_API_URL=https://api.data.gov.in/resource/9ef84268-d588-465a-a308-a864a43d0070
MARKET_API_KEY=your-market-api-key-here

# Alternative names for backward compatibility
AGMARK_API_URL=
AGMARK_API_KEY=

# -----------------------------------------------------------------------------
# OPTIONAL SERVICES
# -----------------------------------------------------------------------------
# Weather API (if using external weather service)
# WEATHER_API_KEY=your-weather-api-key-here
# WEATHER_API_URL=https://api.openweathermap.org/data/2.5

# Government Schemes API
# MYSCHEME_API_URL=https://api.myscheme.gov.in/schemes
# MYSCHEME_API_KEY=your-myscheme-api-key-here

# -----------------------------------------------------------------------------
# RATE LIMITING
# -----------------------------------------------------------------------------
# RATE_LIMIT_WINDOW_MS=60000
# RATE_LIMIT_MAX_REQUESTS=120
